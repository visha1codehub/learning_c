#include <stdio.h>

int main(int argc, char const *argv[])
{
    /* code */
    int a;
    int b;

    a = 36;
    b = 5;

    printf("a + b = %d\n",a+b);
    printf("a - b = %d\n",a-b);
    printf("a * b = %d\n",a*b);   //%d, %f, %i//
    printf("a / b = %d\n",a/b);


    return 0;
}
